[project]
name = "jurbas-code"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
env-file = ".env"
dependencies = [
    "openai>=2.42.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]
